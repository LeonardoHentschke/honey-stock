# =============================================================================
# Honey Stock — Variáveis de ambiente
# Copie este arquivo para .env e preencha com os valores do seu projeto Supabase
# NUNCA commite o .env — ele está no .gitignore
# =============================================================================

# Supabase — encontre em: Project Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<ANON_KEY>

# Expo — encontre em: expo.dev → Project → Project ID
EXPO_PUBLIC_PROJECT_ID=<EXPO_PROJECT_ID>
